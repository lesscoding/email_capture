<div class="block">
  <div class="secondary-navigation">
    <ul class="wat-cf">
      <li class="first"><%= link_to pat(:list), url(:visitors, :index) %></li>
      <li class="active"><%= link_to pat(:new), url(:visitors, :new) %></li>
    </ul>
  </div>
  <div class="content">
    <h2 class="title"><%= pat(:new) %> <% mt(:visitor) %></h2>
    <div class="inner">
      <% form_for :visitor, url(:visitors, :create), :class => :form do |f| %>
        <%= partial "visitors/form", :locals => { :f => f } %>
      <% end %>
    </div>
  </div>
</div>
<% content_for :sidebar, partial("base/sidebar") %>
